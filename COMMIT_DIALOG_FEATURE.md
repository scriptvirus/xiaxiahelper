# Commit 确认对话框功能说明

## ✨ 新功能

点击"发布到服务器"时，如果检测到本地有代码改动，会自动弹出确认对话框。

## 📸 功能演示

### 触发条件
- 点击"发布到服务器"按钮
- 本地有未提交的代码改动

### 对话框内容
```
┌─────────────────────────────────────────┐
│ 📝 确认代码提交                          │
├─────────────────────────────────────────┤
│ 检测到以下代码变动，请输入本次提交说明：  │
│                                         │
│ ┌─────────────────────────────────┐   │
│ │ M  src/components/Button.tsx    │   │
│ │ A  src/utils/helpers.ts         │   │
│ │ D  old/legacy.js                │   │
│ │ ?? new-feature.tsx              │   │
│ │ ... 还有 6 个文件                │   │
│ └─────────────────────────────────┘   │
│                                         │
│ 提交说明 *                              │
│ ┌─────────────────────────────────┐   │
│ │ 修复登录页面样式问题              │   │
│ └─────────────────────────────────┘   │
│                                         │
│         [取消]  [确认并发布]            │
└─────────────────────────────────────────┘
```

## 🎯 功能特点

### 1. 智能检测
- 自动检测是否有代码改动
- 没有改动时直接发布，不弹框
- 有改动时才显示确认对话框

### 2. 文件列表
- 显示前 10 个变动的文件
- 超过 10 个时显示"... 还有 N 个文件"
- 文件状态标记：
  - `M` - 修改
  - `A` - 新增
  - `D` - 删除
  - `??` - 未跟踪

### 3. 输入优化
- 自动填充默认提交说明
- 输入框自动聚焦并选中文本
- 支持回车键快速确认
- 支持 ESC 键取消（可选）

### 4. 验证保护
- 不允许提交空白说明
- 空白时输入框变红提示
- 取消操作不会发布

### 5. 默认提交信息
- 如果之前输入过提交说明，使用之前的
- 如果没有，使用默认格式：`desktop deploy 2026-07-28 15:30:45`
- 可以在对话框中修改

## 💡 使用场景

### 场景 1：快速修复
```
1. 修改代码
2. 点击"发布到服务器"
3. 弹框显示改动文件
4. 输入"修复登录bug"
5. 点击"确认并发布"
6. ✅ 自动 commit、push、部署
```

### 场景 2：多文件改动
```
1. 修改了 15 个文件
2. 点击"发布到服务器"
3. 弹框显示前 10 个文件 + "还有 5 个文件"
4. 输入详细的提交说明
5. 确认发布
```

### 场景 3：没有改动
```
1. 所有代码已提交
2. 点击"发布到服务器"
3. ✅ 直接发布，不弹框
```

### 场景 4：取消发布
```
1. 点击"发布到服务器"
2. 看到改动文件后发现不对
3. 点击"取消"
4. ✅ 不会发布，可以继续修改代码
```

## 🔧 技术实现

### 检测流程
```javascript
async function runNativeDeploy() {
  // 1. 调用 project_status 获取代码状态
  const status = await invoke("project_status", { projectPath });
  
  // 2. 检查是否有改动
  const hasChanges = status.status && status.status.trim() !== "";
  
  // 3. 如果有改动，显示对话框
  if (hasChanges) {
    const result = await showCommitDialog(defaultMessage, status.status);
    if (!result.ok) return; // 用户取消
    message = result.message;
  }
  
  // 4. 继续发布流程
  await invoke("native_deploy", { message, ... });
}
```

### 对话框特性
- 使用 Promise 实现异步等待
- 模态遮罩防止误操作
- 聚焦和选中优化用户体验
- 回车键快捷操作

## 📋 与主界面输入框的关系

### 主界面的"发布说明"输入框
- 作用：提供默认的 commit 信息
- 用户可以提前输入常用的提交说明
- 对话框会预填充这个值

### 对话框的输入框
- 作用：最终确认或修改 commit 信息
- 可以覆盖主界面的输入
- 确认后会更新主界面的输入框

### 交互流程
```
主界面输入框 → 对话框预填充 → 用户修改 → 保存到主界面
```

## 🎨 样式设计

### 对话框样式
- 居中显示
- 半透明遮罩背景
- 圆角卡片设计
- 清晰的按钮层次

### 文件列表样式
- 浅色背景区分
- 等宽字体显示
- 滚动查看（最高 200px）
- 边框和圆角

### 按钮样式
- 主按钮：蓝色高亮
- 次要按钮：灰色边框
- 悬停效果
- 禁用状态

## 🔍 测试建议

### 测试 1：有改动的情况
```bash
# 修改一个文件
echo "test" >> README.md

# 点击"发布到服务器"
# 预期：显示对话框，列出 README.md
```

### 测试 2：没有改动
```bash
# 确保所有文件已提交
git status
# 应该显示：working tree clean

# 点击"发布到服务器"
# 预期：直接发布，不显示对话框
```

### 测试 3：修改提交信息
```bash
# 修改文件
# 点击"发布到服务器"
# 在对话框中输入自定义说明
# 预期：使用自定义说明提交
```

### 测试 4：取消操作
```bash
# 修改文件
# 点击"发布到服务器"
# 点击"取消"
# 预期：不发布，日志显示"用户取消发布"
```

### 测试 5：空白验证
```bash
# 修改文件
# 点击"发布到服务器"
# 清空输入框，点击确认
# 预期：输入框变红，不允许提交
```

## 📦 版本信息

- 添加时间：2026-07-28
- 版本：v0.4.0+
- 平台：Windows / macOS / Linux

## 🚀 未来优化

可选的改进方向：
- [ ] 添加 ESC 键取消支持
- [ ] 显示完整的 diff 统计
- [ ] 支持多行提交信息
- [ ] 保存最近使用的提交说明
- [ ] 显示提交历史建议

---

## 📝 总结

这个功能让发布流程更加清晰和可控：
- ✅ 用户明确知道要提交什么
- ✅ 可以写更有意义的 commit 信息
- ✅ 减少误操作
- ✅ 提升代码管理质量
