# 安全审查报告

## 审查日期
2026-07-28

## 审查范围
虾虾发布助手桌面端应用程序

---

## ✅ 已验证的安全措施

### 1. GitHub Token 保护
- ✅ **加密存储**：使用 AES-256-GCM 加密算法
- ✅ **本机绑定**：加密密钥基于机器特征（用户名 + HOME 路径）
- ✅ **不会泄露到日志**：检查代码，未发现 token 输出到用户界面或日志
- ✅ **不会提交到 Git**：配置文件存储在系统目录，不在项目内
- ✅ **仅保存到本机**：
  - macOS: `~/Library/Application Support/xiaxia-publish-assistant/secrets.enc.json`
  - Windows: `%APPDATA%\xiaxia-publish-assistant\secrets.enc.json`

### 2. SSH 私钥管理
- ✅ **硬编码但不泄露**：私钥编译到二进制中，不以明文形式存在于源码仓库
- ⚠️ **反编译风险**：技术人员可以从编译后的程序中提取私钥
- ✅ **内存认证**：使用 `userauth_pubkey_memory` 直接从内存认证，不写入临时文件
- ✅ **不会泄露到日志**：私钥内容不会输出到用户界面

### 3. 凭据传输安全
- ✅ **无网络传输**：GitHub Token 只在本机使用，通过 Git 库直接认证
- ✅ **SSH 加密通道**：服务器连接使用 SSH 协议，数据传输加密
- ✅ **TLS 连接**：Git 操作通过 HTTPS/SSH 进行，不会明文传输

### 4. 用户界面安全
- ✅ **密码字段**：GitHub Token 输入框使用 `type="password"`，不可见
- ✅ **HTML 转义**：所有用户输入和动态内容都经过 `escapeHtml()` 处理
- ✅ **无 XSS 风险**：前端代码使用安全的字符串拼接，没有 `innerHTML` 注入风险

### 5. 文件系统安全
- ✅ **权限检查**：配置文件目录自动创建，有适当的权限控制
- ✅ **路径验证**：用户选择的路径经过验证，不会访问系统敏感目录

---

## ⚠️ 已知风险与建议

### 1. SSH 私钥硬编码
**风险等级**：中等

**风险描述**：
- 私钥编译在程序中，任何获得程序的人都可能通过反编译提取私钥
- 一旦程序分发出去，私钥无法撤回

**建议措施**：
- ✅ 不要公开分发此程序
- ✅ 定期更换服务器 SSH 密钥对
- ✅ 在服务器端配置防火墙，限制 SSH 访问来源 IP
- ✅ 使用服务器日志监控异常登录

### 2. 单一 GitHub Token
**风险等级**：低

**风险描述**：
- 如果 Token 被盗，攻击者可以访问关联的仓库

**建议措施**：
- ✅ 创建专用的 GitHub Token，仅授予最小权限（repo 权限）
- ✅ 定期更换 Token（建议每 3-6 个月）
- ✅ 在 GitHub 监控 Token 使用情况
- ✅ 如果怀疑泄露，立即在 GitHub 撤销

### 3. 本机加密密钥
**风险等级**：低

**风险描述**：
- 加密密钥基于本机特征生成，本机用户可以解密配置文件

**说明**：
- 这是设计上的权衡，目的是防止配置文件被误提交到 Git
- 不是企业级的密钥管理方案
- 适合单人使用场景

---

## 🔒 安全最佳实践

### 开发者
1. ✅ 不要将包含 SSH 私钥的源代码提交到公开仓库
2. ✅ 使用 `.gitignore` 忽略编译产物
3. ✅ 定期审查代码，确保没有意外的敏感信息泄露
4. ✅ 使用 GitHub Secret Scanning 监控泄露

### 用户
1. ✅ 首次使用时创建专用的 GitHub Token
2. ✅ 不要在公共场所使用程序（避免屏幕录制）
3. ✅ 不要将程序分享给不信任的人
4. ✅ 定期检查 GitHub 的 Token 使用日志
5. ✅ 电脑设置屏幕锁定密码

---

## 📊 检查清单

- [x] GitHub Token 加密存储
- [x] SSH 私钥不泄露到日志
- [x] 用户输入 HTML 转义
- [x] 密码字段正确设置
- [x] 配置文件不在 Git 目录
- [x] 无明文密码存储
- [x] 网络传输使用加密协议
- [x] 文件路径验证
- [x] 错误消息不泄露敏感信息
- [x] 前端无 XSS 漏洞
- [x] 后端无 SQL 注入风险（不使用数据库）
- [x] 无命令注入风险（使用库而非 shell）

---

## 🎯 结论

**总体评估**：✅ 安全性良好

该应用程序在设计上考虑了主要的安全风险，采用了适当的加密和保护措施。主要风险点（SSH 私钥硬编码）是设计权衡的结果，在单人使用、不公开分发的场景下是可接受的。

**推荐使用场景**：
- ✅ 个人项目部署
- ✅ 小团队内部使用（受信任成员）
- ❌ 不推荐公开分发
- ❌ 不推荐多租户环境

**安全等级**：B+（良好）

---

## 📝 修订历史

- 2026-07-28：初始审查，移除不必要的 SSH key 和服务器密码输入框
